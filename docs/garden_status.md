# Garden Dataset/Asset Status (Phase 0)

## Scope
- Target scene: `garden` only.
- Dataset family: `Mip-NeRF360`.

## Resolved paths
- Raw scene root: `data/mipnerf360/garden`
- Artifact scene root: `artifacts/garden`
- Paired training data root: `pairs_real/garden`
- Active adapter checkpoint used for final Garden outputs: `checkpoints/garden/adapter_best.pt`
- Model path config: `configs/model_paths.yaml`

## View counts
- Artifact metadata (`artifacts/garden/metadata.json`) reports:
  - total views: `185`
  - train split: `161`
  - test split: `24`
- Confirmed on disk:
  - `artifacts/garden/clean/*.png`: `185`
  - `artifacts/garden/rate_0.100/*.png`: `185`
  - `pairs_real/garden/rate_0.100/train/clean/*.png`: `161`
  - `pairs_real/garden/rate_0.100/test/clean/*.png`: `24`

## HQ / GT source
- Current HQ images used by restoration demo/training are from `artifacts/garden/clean`.
- These are copied from the scene image folder selected by `nifi/gs/compressor.py::list_scene_images`.
- For this scene, the first matching folder is `data/mipnerf360/garden/images_4`, so HQ is dataset imagery (not a freshly rendered HQ 3DGS output).

## LQ / compressed source
- Current LQ images are generated by the proxy artifact synthesizer:
  - `nifi/artifact_synthesis/compression_simulation.py`
  - `nifi/gs/compressor.py::Proxy3DGSCompressor`
- Compression is synthetic JPEG/downsample/quantization degradation (rate folders like `rate_0.100`), not a real HAC++ compressed 3DGS decode path in the current Garden assets.
- `artifacts/garden/run_manifest.json` confirms `--method proxy` for the Garden artifact build.

## Garden-only demo target
- Garden demo output directory used for final assets: `outputs/demo/garden/garden`
- Garden qualitative export target: `docs/assets/qualitative/garden/garden`
